const baseRoot = 'd:/boa/one.scripts';
const buildSuffix = '_build';
const buildRoot = `${baseRoot}${buildSuffix}`;
const deployRootTemplate = `{Root}/{ModuleName}/{SubModuleName}`;

export { baseRoot, buildRoot, buildSuffix, deployRootTemplate };
